export class OptionViewModel {
  constructor(id, question_id, option_text) {
    this.id = id;
    this.question_id = question_id;
    this.option_text = option_text;
  }
}
export default OptionViewModel;